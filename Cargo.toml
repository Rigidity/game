[package]
name = "defaria"
version = "0.1.0"
edition = "2021"

[features]
default = ["dev"]
dev = ["bevy/dynamic_linking"]

[dependencies]
avian3d = { version = "0.2.1", features = ["collider-from-mesh"] }
bevy = "0.15.1"
noise = "0.9.0"

[profile.dev]
opt-level = 1
